Run command from project dir:
    ruby runner.rb

Before scrape starting create work directory and sub dir "todo" with todo-file('todo' is hardcoded file name ), which contains urls.

There is CONFIGURATION_HASH in runner.rb, where we can define work_dir, counts of threads for parser and scraper.